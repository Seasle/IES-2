export class Region {
    constructor (
        public name: string,
        public population: number,
        public area: number
    ) {}
}

export class City {
    constructor(
        public name: string,
        public population: number
    ) {}
}